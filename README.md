# mani
mani - An autonomous operating room assistant 

## Usage
Run `pip install .` from repo directory to install version matched dependencies.

If on windows use
```
python sim.py
```

If on Mac use
```
mjpython sim.py
```

To include full system with ORCA mounted to frank add `-fr` flags
